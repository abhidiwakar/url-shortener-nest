# Keep NestJS peer/runtime packages resolvable from hoisted root node_modules.
# Without this, npm workspaces can nest backend-only deps under apps/backend while
# @nestjs/core is hoisted to the repo root, breaking Nest bootstrap.
install-strategy=hoisted
